namespace CasebookGame.Data
{
    public enum EvidenceDisplayMode
    {
        Default,   // standard RawImage viewer (zoom/pan)
        Document,  // sprite bg + sepia typed text overlay
        Terminal,  // sprite bg + green monospace text overlay
        Keycard,   // sprite bg + corporate ID text overlay
    }
}
